package api

/* import (
	"github.com/gin-gonic/gin"
	"go.opentelemetry.io/otel"
)

func SomeHandler(c *gin.Context) {
	tracer := otel.Tracer("vault-api")
	ctx, span := tracer.Start(c.Request.Context(), "Somehandlers")
	defer span.End()

	// We'll use ctx for outgoing calls
} */
